import { z } from "zod";
const environmentSchema = z.object({ NODE_ENV: z.enum(["development", "test", "production"]).default("development"), API_PORT: z.coerce.number().int().min(1).max(65_535).default(4000), WEB_ORIGIN: z.string().url().default("http://localhost:3000"), JWT_SECRET: z.string().min(32).default("local-development-secret-change-me"), DATABASE_URL: z.string().min(1).optional() });
export function validateEnvironment(input: NodeJS.ProcessEnv) { const parsed = environmentSchema.safeParse(input); if (!parsed.success) throw new Error(`Invalid environment configuration: ${parsed.error.issues.map((issue) => issue.path.join(".")).join(", ")}`); if (parsed.data.NODE_ENV === "production" && !parsed.data.DATABASE_URL) throw new Error("DATABASE_URL is required in production"); return parsed.data; }
